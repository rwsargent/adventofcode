import re
def read_input(filename):
    string_size = 0, memory_size = 0
    hexi = re.compile("\\[a-f]{2}")
    espcae
    with open(filename) as file:
        for line in file:
            string_size += len(line)
            line = hexi.replace(line, "*")
            qa
    return None
        
def execute_part_one(input):
    return None

def execute_part_two(input):
    return None

def get_expected_results_map():
    return { 'test.txt' : (None, None)}
